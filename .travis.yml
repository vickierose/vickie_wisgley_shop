language: node_js
node_js:
  - "node"
cache:
  directories:
    - "node_modules"

install: npm install

script:
  - npm run build

before_deploy:
  - test $TRAVIS_TEST_RESULT = 0
    && bash cp -f .gitignore-heroku .gitignore

deploy: 
  provider: heroku
  api_key:
    secure: $HEROKU_KEY
  app: vickiewisgleyshop
  on: resetup